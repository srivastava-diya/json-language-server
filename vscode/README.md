# Hyperjump - JSON VSCode Extension

A VSCode extension for the JSON Language Server.

## Disabling JSON Language Features

This is a replacement for VSCode's built-in "JSON Language Features" extension.
You'll need to disable that extension in order to avoid conflicts. In the
"Extensions" tool, search for `@builtin json language features` to find the
extension. Click the "Disable" button to disable it.
